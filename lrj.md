# CLAUDE.md — 아라온 그로스 (Claude Code 세션 규칙)

> 세션 시작 시 반드시 읽기: AGENTS.md → https://crocdeucation-rgb.github.io/image/state.md
> 아래 5원칙은 매 응답 전에 상기한다. 실제 겪은 실패에서 뽑은 것이라 예외 없다.

## 문제 해결 5원칙 (반복 실패 방지 — 절대)

1. **보기 전에 답하지 않는다.**
   증상을 보면 원인을 단정하지 말고, 관련 파일/데이터부터 열어 확인한 뒤 답한다.
   사용자가 파일을 줬거나 "이상하다"고 하면 → 그 파일을 grep/열람하는 것이 *첫 행동*.
   파일이 없으면 GitHub raw URL(raw.githubusercontent.com/.../main/파일명)로 받아서 본다.
   (왜: dashboard.html ON/OFF 원인을 파일 안 읽고 30분 추측 → 실제론 파일 안의 ON_CAMPAIGNS/OFF_CAMPAIGNS 배열이었음)

2. **원인 후보를 2~3개 세우고 하나씩 배제한다.**
   "이거겠지" 하나로 응급조치하지 말고, 가능성을 나열해 코드/데이터로 지운 뒤 답한다.
   단편적 표면 수정 전에 "이게 근본 원인인가, 증상인가"를 먼저 자문한다.

3. **사용자가 "아니다/그거 아니야"라고 하면 내 가설을 버린다.**
   같은 프레임 고수 금지. 처음부터 다시 본다. 사용자의 반박 = 내 가설이 틀렸다는 신호.
   (왜: "html이 자체적으로 하는 거 아니냐"는 지적을 여러 번 무시하고 엉뚱한 dash.gs만 팠음)

4. **"해결됐다 / 정상이다 / 완벽하다"는 확인 전에 말하지 않는다.**
   확인 = 코드를 실제로 열어봤거나, 사용자가 화면으로 확인해준 경우만.
   검증 못 하면 단정 대신 "확인이 필요하다"고 말한다.
   (왜: 오프라인·노션앱·캐시 등을 확인 없이 "정상/원인"이라 단정 → 전부 틀렸음)

5. **막히면 추측을 쌓지 말고 멈춰서 정확한 상태를 묻는다.**
   틀린 추측을 계속 쌓으면 사용자 시간을 태운다. 모르면 "어디서 막히는지" 정확히 물어본다.

## 대시보드 고정 사실 (반복 착오 방지)
- 대시보드 캠페인 ON/OFF = **dashboard.html의 ON_CAMPAIGNS / OFF_CAMPAIGNS 배열**이 결정.
  (dash.gs의 status는 죽은 값 — 시트에 안 쓰이고 아무 데서도 안 읽힘. 건드려도 효과 없음)
- getLeadStats는 실시간 아님 — 실행해야 시트 갱신됨. "반영 안 됨"은 재실행/시점부터 의심.
- 노션 필드 rich_text는 여러 조각으로 쪼개질 수 있음 → `.map(t=>t.plain_text).join('')`로 전체 읽기.
  `rich_text[0]`만 읽으면 값이 잘려 매칭 실패 (리드 중복·집계 누락의 근본 원인).
- 모바일 대시보드 CSS 수정 시 노션 앱 임베드에서 스크롤 충돌 가능 → 화면 확인 후 반영.
